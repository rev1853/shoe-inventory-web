previously i have developed a frontend project (only the ui and the navigation, basically a prototype)
i want you to use that frontend project and move to this laravel project so i can run both backend and frontend in just a singgle command
you can find the project here /var/www/web/learn-laravel/Shoeinvetoryui
you dont need to use intertia or whatever, what i want is the frontend project moved/integrated in this laravel project
then i can run run the frontend and the laravel project, but i want you to make that automatic, so i can run one command to run both project

then i want you to create and run migration based on the DATABASE.md, you need to read and create the migration
after doing the migration you need to create the models, and please use shoes_data.json to create the seeder
you may see that some images are using url, i want you to download those iamge and store it in the designated folder based on laravel
so next when i add image that image will stored at the same folder, and in database you can just use the filename
